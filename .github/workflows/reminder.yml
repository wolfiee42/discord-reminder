name: Test Discord Reminder

on:
  workflow_dispatch: {} # allows manual run
  schedule:
    - cron: "* * * * *" # every minute

jobs:
  ping-vercel:
    runs-on: ubuntu-latest
    steps:
      - name: Call Vercel Reminder API
        run: curl -s https://discord-reminder-brown.vercel.app/api/reminder
